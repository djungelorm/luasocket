name: Build

on:
  push:
    branches:
      - master
  pull_request:

jobs:
  build:
    name: Test ${{ matrix.luaVersion }} on ${{ matrix.platform }}
    strategy:
      fail-fast: false
      matrix:
        luaVersion: [ "5.4", "5.3", "5.2", "5.1", "luajit", "luajit-openresty" ]
        platform: [ "ubuntu-22.04", "macos-14", "windows-2022" ]
    runs-on: ${{ matrix.platform }}
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - name: Setup ’msvc’
        if: ${{ startsWith(matrix.platform, 'windows') && !startsWith(matrix.luaVersion, 'luajit') }}
        uses: ilammy/msvc-dev-cmd@0b201ec74fa43914dc39ae48a89fd1d8cb592756 # v1
      - name: Setup ‘lua’
        uses: luarocks/gh-actions-lua@85e2287697275df96334743ac654241a8fd34ab3 # v13
        with:
          luaVersion: ${{ matrix.luaVersion }}
          buildCache: false
      - name: Setup ‘luarocks’
        # master branch until tagged release has luajit & msvcrt fixes
        uses: luarocks/gh-actions-luarocks@95d2308a0aff36ad31ebadca493ed14853399842 # master
      - name: Make and install
        run: |
          luarocks make -- luasocket-scm-3.rockspec
        env:
          DEBUG: DEBUG
      - name: Run regression tests
        shell: bash
        run: |
          cd test
          lua hello.lua
          lua testsrvr.lua > /dev/null &
          lua testclnt.lua
          lua stufftest.lua
          lua excepttest.lua
          lua test_bind.lua
          lua test_getaddrinfo.lua
          lua test_unix.lua
          lua ltn12test.lua
          lua mimetest.lua
          lua urltest.lua
          lua test_socket_error.lua
          kill %1

  solution:
    name: Build luasocket.sln
    runs-on: windows-2022
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - name: Setup ’msvc’
        # Also puts MSBuild on PATH, by way of vcvarsall
        uses: ilammy/msvc-dev-cmd@0b201ec74fa43914dc39ae48a89fd1d8cb592756 # v1
      - name: Setup ‘lua’
        uses: luarocks/gh-actions-lua@85e2287697275df96334743ac654241a8fd34ab3 # v13
        with:
          luaVersion: "5.4"
          buildCache: false
      - name: Build the solution
        shell: bash
        # The projects name the v141 toolset, which the runner does not carry, and
        # Lua.props points at a path on the author's machine, so both are given here
        # rather than changed in tree: a property on the command line wins over one
        # the project sets, and a developer opening the solution keeps their own.
        run: |
          root=$(pwd -W)
          MSBuild.exe luasocket.sln \
            -p:Configuration=Release \
            -p:Platform=x64 \
            -p:PlatformToolset=v143 \
            -p:LUAINC="$root/.lua/include" \
            -p:LUALIB="$root/.lua/lib" \
            -p:LUALIBNAME=lua54.lib
      - name: Check the modules were built
        shell: bash
        run: |
          ls -lR x64/Release
          for dll in socket/core socket/unix mime/core; do
            test -f "x64/Release/$dll.dll" \
              || { echo "x64/Release/$dll.dll was not built"; exit 1; }
          done
